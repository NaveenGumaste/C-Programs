#include <stdio.h>
int prime(int num[]);
int reversed(int a);
int main()
{
    int c,rev;
    printf("Enter the array to check prime number:");
    scanf("%d",i);
    printf("Enter the number to be reversed: ");
    scanf("%d",rev);
    c =prime(num);
    printf("Prime num in the arrat")
}
