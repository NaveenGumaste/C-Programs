
#include <stdio.h>
#include <stdlib.h>
#define MAX 5

int queue[MAX];

int main()
{
    int queue[MAX]={65, 72,18,15, 21};

}

void insert(int value)
{
    int front ,rear;
    if(rear+1==MAX)
    {
        printf("overflow full");
    }
    else
    {
        if (front==-1 && rear ==-1)
    }
    front=0;
    rear=0;

}
