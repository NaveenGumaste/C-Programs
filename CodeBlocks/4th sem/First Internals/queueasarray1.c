#include <stdio.h>
#include <stdlib.h>
#define MAX 5

int queue[MAX];
int front=-1;
int rear=-1;

void enqueue(int value);
void dequeue();
void display ();

Int main()
{
 enqueue(65);
 enqueue(72);
 enqueue(18);
 enqueue(15);
 enqueue(21);
 display();

 dequeue();
 display();
}

void enqueue (int value)
{

}

